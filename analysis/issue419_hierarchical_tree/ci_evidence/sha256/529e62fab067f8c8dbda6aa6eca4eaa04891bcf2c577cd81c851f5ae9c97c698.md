# #419 — pre-push preparation of the repaired HEAD (`poker-issue419-pre-push-preparation/v1`)

This record freezes the **repaired** #419 HEAD as pre-push preparation and keeps
the two kinds of evidence apart, without decoration.

## 1. CI evidence — absent

`ci_observation.status = NOT_OBSERVED`. The authoritative run is
« Execute issue 419 hierarchical exact tree »
(`.github/workflows/issue-419-hierarchical-exact-tree.yml`); it has never produced
a run at any head, because the workflow is absent at the current remote PR head
and this branch has not been pushed past it. `run_url` and `run_conclusion` are
`null`. The authority is the source bundle
`analysis/issue419_hierarchical_tree/ci_evidence/CI_EVIDENCE.json`
(`764b7f6d…`), which this record does not edit.

Nothing local may promote that status — not a replay, not a declarative file, not
the PR body, not this record.

## 2. Local evidence — non-authoritative

Frozen head: `0f9a6eb0e53a5191a2302d5eabb175ae267f7c24` (`0f9a6eb`, branch
`n8n/issue-419/task-backlog-g95`, not pushed, not a merge commit). 21 commands
were replayed in this sandbox: **20 green, 1 red**.

| command | exit | observed |
| --- | --- | --- |
| `python3 tools/simulation/issue419_exact_tree_preflight.py --check` | 0 | `check=PASS` (frozen v1 custody green, v2 bundle reproducible) |
| `python3 tools/training/finalize_hierarchical_exact_tree_decision.py --check` | 0 | `UNRESOLVED_HIERARCHICAL_TREE_GAP`, `BLOCKED_SCIENTIFIC` |
| `tests/simulation/test_issue419_exact_tree_preflight.py` | 0 | OK |
| `tests/preflop/test_model_a_sizing_hierarchical.py` | 0 | 23 passed |
| `tests/training/test_hierarchical_train_fit.py` | 0 | OK |
| `tests/training/test_hierarchical_validation_evaluation.py` | 0 | OK |
| `tests/training/test_hierarchical_terminal_decision.py` | 0 | OK |
| `tests/training/test_hierarchical_model_spec.py` | 0 | OK |
| `tests/training/test_hierarchical_candidate_contract.py` | 0 | OK |
| `tests/training/test_hierarchical_exact_context_contract_doc.py` | 0 | OK |
| `tests/training/test_hierarchical_tree_sparsity_parity.py` | 0 | OK |
| `tests/training/test_frozen_validation_protocol.py` | 0 | OK |
| `tests/training/test_frozen_validation_protocol_v2.py` | 0 | OK |
| `tests/training/test_raise_sizing_frontier_resolution.py` | 0 | OK |
| `tests/training/test_issue419_hierarchical_exact_tree.py` | **1** | **FAILED (failures=2, errors=1)** |
| `tests/test_github_workflow_audit.py` | 0 | OK (15 tests); 0 uncovered paths, 0 fabricated paths, no-op tuple == `ISSUE_419_SUITES ∪ ISSUE_419_EXTRA_SUITES ∪ ISSUE_419_CI_SUITES` |
| `tests/ci/test_consolidation_decision.py` | 0 | OK |
| `python3 tools/training/build_hierarchical_evidence_integrity_correction_v2.py --check` | 0 | `result=PASS` |
| `python3 tools/audit_github_workflows.py --inventory` + byte compare | 0 | `analysis/workflow_audit/workflows.json` regenerated byte-identically |
| `python3 tools/audit_active_workflow_dag.py --check` | 0 | PASS (55 workflows; 14 decision scenarios) |
| `validation_order_guard.validation_result_artifacts()` | 0 | exactly the two frozen VALIDATION artifacts |

### The one red check, recorded rather than hidden

`PYTHONPATH=. python3 tests/training/test_issue419_hierarchical_exact_tree.py` →
`FAILED (failures=2, errors=1)`. Cause: the integration bundle
`analysis/issue419_hierarchical_tree_v2/` still binds `EXACT_TREE_PREFLIGHT_V2.json`
to `2c07ae32…` and `DECISION_V2.json` to `ac291b9e…`, the revisions that existed
when it was authored; `9859c46` and `0f9a6eb` later regenerated those bundles, so
its pins no longer describe the bytes on disk. It touches no frozen byte and it is
owned by the integration-bundle task (`backlog-oso` / T7), not by this record.

## 3. What this record supersedes, and what it never edits

`ci_evidence/SUMMARY.md` states that all 15 suites return `0` in its sandbox. That
was true at *its* head and is stale at this one. It is another task's recorded
observation, so it is **superseded here, not edited**: a recorded observation is
replaced by a new record, never rewritten in place. The delta is exactly the one
red check above.

## 4. Boundaries

No frozen byte written, no predecessor record edited, no root index mutated, no
active model pointer moved, VALIDATION not re-read, TEST unreachable, no hand
history parsed, no Hero EV executed, #367 not run, no threshold, prior, pooling
limit or admission rule changed. The local replay is not merge evidence.

Artifact digests are in `ARTIFACTS.json` of this bundle.
