# Source

Application and engine source files belong here when they are split out of the historical single-file HTML application.

Until that refactor exists, complete runnable builds remain under `user/releases/` and migration/build utilities under `tools/`.
