# Project Status

<!-- generated by tools/validate_project_state.py --render-status -->

> AUTO-GENERATED from `.project/capabilities.json` and versioned repository evidence.
> Do not edit manually. Regenerate with `python3 tools/validate_project_state.py --write-status`.

## Validation

- Result: `WARN`
- Errors: `0`
- Warnings: `1`

## Delivery-state summary

| State | Capabilities |
| --- | ---: |
| `CONTRACT_READY` | 2 |
| `SCIENTIFICALLY_SELECTED` | 0 |
| `PRODUCT_INTEGRATED` | 0 |
| `PROMOTED` | 2 |
| `LIVE` | 0 |

## Capabilities

### `target_zoom_population_data` — `CONTRACT_READY`

Certified PokerStars NLHE 100/200 Zoom play-money population boundary and isolated registry entry.

Evidence:
- `PASS` `target-population-certified` proves `CONTRACT_READY` — training/populations/registry.json/populations/pokerstars_nlhe_100-200_zoom_play_6max_v1/status == 'CERTIFIED_DATA_ONLY'

Higher-state limits:
- None.

Administrative contradiction:
- None.

### `trainer_preflop_guidance` — `CONTRACT_READY`

Preflop decision contracts exist, but the current trainer still starts Hero decisions on the flop.

Evidence:
- `PASS` `preflop-decision-contract` proves `CONTRACT_READY` — path exists: docs/preflop-decision-contract.md

Higher-state limits:
- `ACTIVE` `trainer-flop-only-boundary` blocks `PRODUCT_INTEGRATED` — site/trainer.js contains required text

Administrative contradiction:
- Issue #109 is `CLOSED` and claimed `PRODUCT_INTEGRATED`; successor #206; The shipped trainer declares a deliberate v1 boundary where Hero decisions begin on the flop.

### `legacy_mixed_population_engine_v83` — `PROMOTED`

Legacy mixed-format population retains the explicitly promoted v83 engine identity.

Evidence:
- `PASS` `legacy-registry-promoted` proves `PROMOTED` — training/populations/registry.json/populations/legacy_pokerstars_nlhe_100-200_play_6max_mixed_v1/status == 'PROMOTED_LEGACY'
- `PASS` `v83-release-present` proves `PROMOTED` — path exists: user/releases/poker_range_equity_offline_multiway_v83.html

Higher-state limits:
- `ACTIVE` `live-site-unverified` blocks `LIVE` — site/RELEASE.json/publication_verification/status == 'UNVERIFIED_LIVE'

Administrative contradiction:
- None.

### `assembled_static_application` — `PROMOTED`

The assembled static application has a promoted repository/build identity; canonical live deployment remains unverified.

Evidence:
- `PASS` `site-release-promoted` proves `PROMOTED` — site/RELEASE.json/status == 'promoted'

Higher-state limits:
- `ACTIVE` `canonical-live-proof-pending` blocks `LIVE` — site/RELEASE.json/publication_verification/tracked_by_issue == 45

Administrative contradiction:
- None.

## Historical contradictions

- WARN — trainer_preflop_guidance: issue #109 is CLOSED with claimed PRODUCT_INTEGRATED, while versioned evidence declares CONTRACT_READY; tracked by successor issue #206: The shipped trainer declares a deliberate v1 boundary where Hero decisions begin on the flop.

## Validation errors

- None.

## Interpretation

- A closed issue is not evidence that a capability is active, promoted, or live.
- `PASS` on a higher-state limit means the blocking condition is currently evidenced.
- `LIVE` requires explicit canonical-production proof; publication/build metadata alone is insufficient.
