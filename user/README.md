# User-facing artifacts

This directory contains files intended to be consumed directly by the user.

Recommended structure:

- `releases/` — runnable/versioned application builds.
- `docs/` — usage documentation and explanations.
- `exports/` — durable reports/results explicitly worth keeping.

Diagnostic dumps, temporary patches, assistant state and intermediate experiments do not belong here.
